---

sidebar_position: 3

---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import JsExample from '@site/src/components/codeblocks/JsExample';
import GqlExample from '@site/src/components/codeblocks/GqlExample';

# NFTs

After the NFT is minted in the checkout you can access it through couple queries. Due to immutability of NFTs - mutations on them are not possible. You can only query them. 

### Select NFTs

<Tabs>
<TabItem value="js" label="JavaScript">
<JsExample>

```
const nfts = await interactwith.minter.nfts.list({
  pagination: {
    page: 1,
    take: 10,
  },
  sort: {
    by_field: "created_at",
    direction: "desc",
  },
  filter: {
    checkout: "CHECKOUT_ID",
  },
});
```

</JsExample>
</TabItem>
<TabItem value="gql" label="GraphQL">
<GqlExample resource="DassetsNft" method="select"/>
</TabItem>
</Tabs>

### Get NFT

<Tabs>
<TabItem value="js" label="JavaScript">
<JsExample>

```
const nft = await interactwith.minter.nfts.get({
  id: "NFT_ID",
});
```

</JsExample>
</TabItem>
<TabItem value="gql" label="GraphQL">
<GqlExample resource="DassetsNft" method="get"/>
</TabItem>

</Tabs>